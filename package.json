{
  "name": "markanalysis",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.18.0"
  },
  "scripts": {
    "build": "cd server && npx prisma contract emit && cd ../client && npm run build",
    "vercel-build": "cd server && npx prisma contract emit && cd ../client && npm run build",
    "docs:pdf": "node scripts/generate-user-manual-pdfs.js",
    "test:manual-automation": "node scripts/run-manual-automation.mjs",
    "test:manual-automation:prepare": "PREPARE_DB=1 SEED=1 node scripts/run-manual-automation.mjs"
  },
  "dependencies": {
    "@prisma/cli-engine": "^0.3.0",
    "@prisma/orm-postgres": "^8.0.0-rc.11",
    "bcryptjs": "^3.0.2",
    "cookie-parser": "^1.4.7",
    "cors": "^2.8.5",
    "csv-parse": "^5.6.0",
    "dotenv": "^17.4.2",
    "exceljs": "^4.4.0",
    "express": "^5.1.0",
    "jsonwebtoken": "^9.0.2",
    "multer": "^2.0.1",
    "pdfkit": "^0.17.1",
    "pg": "^8.16.3",
    "temporal-polyfill": "^1.0.5"
  },
  "devDependencies": {
    "prisma": "^8.0.0-rc.14"
  }
}
